given the dataset, write code to train xgboost  classifier first with base parameters, log the experiment to mlflow as version 1. name the model fraud_detector_xgb. log metrics: classification report (accuracy, f1 score, precision, recall), auc, and plot the confusion matrix and shap. do everything in a pipeline where the preprocessing step is also wrapped in the pipeline together with the model using columntransformer e.g.,: fraud_pipeline = Pipeline([('preprocessor', preprocessor), ('classifier', xgb_classifier)]). step 2: Step 2: perform hyperparameter tuning and use child runs to log each trial of each parameter combination as a separate child run to mlflow experiment tracking. this should allow comparison of all child runs and identification of the best-perfoming one. step 3: registration of the best model as V2.  train the final model using best optimized hyperparameters and register it as V2, 
promote V1 to archived and V2 to staging